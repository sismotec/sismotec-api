const models = require('../../../../models');
const {NecesidadAcopio, CantidadAcopioRecurso, sequelize} = models;


let handler = (req, res) => {
  const {id_necesidad} = req.body;
};

module.exports = {handler};
